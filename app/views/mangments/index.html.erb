<h1>Listing mangments</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Age</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @mangments.each do |mangment| %>
  <tr>
    <td><%= mangment.name %></td>
    <td><%= mangment.description %></td>
    <td><%= mangment.age %></td>
    <td><%= link_to 'Show', mangment %></td>
    <td><%= link_to 'Edit', edit_mangment_path(mangment) %></td>
    <td><%= link_to 'Destroy', mangment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Mangment', new_mangment_path %>
